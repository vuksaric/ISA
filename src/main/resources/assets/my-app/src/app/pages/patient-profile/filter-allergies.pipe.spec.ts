import { FilterAllergiesPipe } from './filter-allergies.pipe';

describe('FilterAllergiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAllergiesPipe();
    expect(pipe).toBeTruthy();
  });
});
