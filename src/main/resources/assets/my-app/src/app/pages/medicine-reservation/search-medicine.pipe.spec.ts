import { SearchMedicinePipe } from './search-medicine.pipe';

describe('SearchMedicinePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMedicinePipe();
    expect(pipe).toBeTruthy();
  });
});
