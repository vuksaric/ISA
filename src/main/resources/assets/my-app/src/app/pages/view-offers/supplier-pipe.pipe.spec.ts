import { SupplierPipePipe } from './supplier-pipe.pipe';

describe('SupplierPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierPipePipe();
    expect(pipe).toBeTruthy();
  });
});
